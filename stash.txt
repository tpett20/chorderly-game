class Chord {
    constuctor(id, idx, sound) {
        this.id = id;
        this.idx = idx;
        this.sound = sound
    }
}

const cMajor = new Chord('ArrowUp', 0)
const gMajor = new Chord('ArrowRight', 1)
const fMajor = new Chord('ArrowLeft', 2)
const aMinor = new Chord('ArrowDown', 3)

const cMajor = new Audio('sounds/C-Major-GrandPiano.m4a')

console.log(playerSequence[inputIdx], computerSequence[inputIdx])
console.log('inputidx', inputIdx)
console.log('player seq', playerSequence)
console.log('comp seq', computerSequence)